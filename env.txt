# Create a new conda environment (or use venv)
conda create -n bcm_transformer python=3.8
conda activate bcm_transformer

# Install required packages
pip install torch torchtext numpy matplotlib tqdm
